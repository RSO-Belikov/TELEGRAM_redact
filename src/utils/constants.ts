export const MAX_MESSAGE_LENGTH = 4096; // Максимальная длина сообщения Telegram

export const MARKDOWN_CHARS = ['*', '_', '`', '[', ']', '(', ')', '~', '|', '>', '#', '+', '-', '=', '{', '}', '.'];

export const COMMON_FIXES = [
  { search: /\s+/g, replace: ' ' },                    // Множественные пробелы на один пробел
  { search: /[\u200B-\u200D\uFEFF]/g, replace: '' },  // Удалить пробелы нулевой ширины
  { search: /['']/g, replace: "'" },                   // Нормализовать кавычки
  { search: /[""]/g, replace: '"' },                   // Нормализовать двойные кавычки
  { search: /\.{3,}/g, replace: '…' },                // Три точки на многоточие
  { search: /\s+([.,!?;:])/g, replace: '$1' },        // Удалить пробелы перед знаками препинания
  { search: /([.,!?;:])\s*/g, replace: '$1 ' },       // Нормализовать пробелы после знаков препинания
];